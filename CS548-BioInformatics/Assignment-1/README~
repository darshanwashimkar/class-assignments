Compilation:
g++ -std=c++11 gassembler.cpp

Running the code:
./gassembler.exe -k 31 --r1 file-1 --r2 file-2

Program generate the output on stdout, you will need to dump this output to the file, you can use following command.
./gassembler.exe -k 31 --r1 file-1 --r2 file-2 > OUTPUT_FILE

Program also creates a file exec_time.txt, which note down the execution start and end time.
